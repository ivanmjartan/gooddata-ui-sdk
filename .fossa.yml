---
version: 3
project:
  id: gooddata-ui-sdk

telemetry:
  scope: 'off'

targets:
  only:
    - type: pnpm
      path: common/config/rush
  exclude:
    - type: pnpm
      path: applink



  #only:
  #  - type: bundler
  #    path: libs/sdk-model
  #only:
  #  - type: pnpm
  #    path: common/config/rush
  #exclude:
    #- type: bundler
    #  path: tools/applink
